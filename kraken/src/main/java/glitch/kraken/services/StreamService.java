package glitch.kraken.services;

import glitch.common.api.BaseURL;
import glitch.common.api.HttpClient;

public class StreamService {
    public StreamService(BaseURL base, HttpClient httpClient) {
    }
}
