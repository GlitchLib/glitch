package io.glitchlib.v5.model

enum class ClipPeriod {
    DAY,
    WEEK,
    MONTH,
    ALL
}

enum class Direction {
    ASC,
    DESC
}

enum class Sorting {
    CREATED_AT,
    LAST_BROADCAST,
    LOGIN
}

enum class StreamType {
    LIVE,
    PLAYLIST,
    ALL
}

enum class VideoPeriod {
    WEEK,
    MONTH,
    ALL
}

enum class VideoSort {
    VIEWS,
    TIME
}
